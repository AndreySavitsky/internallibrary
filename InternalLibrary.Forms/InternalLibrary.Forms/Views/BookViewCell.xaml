<ViewCell
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:model="clr-namespace:InternalLibrary.Forms.Models"
    x:Class="InternalLibrary.Forms.Views.BookViewCell"
    x:DataType="model:Book">
        <ViewCell.View>
            <StackLayout Margin="10">
                <Label
                        Text="{Binding Title}"
                        Style="{StaticResource BookTitleTextStyle}"/>
                <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                    <Label
                        Text="{Binding Status}"
                        Style="{StaticResource BookISBNTextStyle}"/>
                    <Label
                        Text="{Binding Email}"
                        Style="{StaticResource BookEmailTextStyle}"/>
                    <Label
                        Text="{Binding Description}"
                        Style="{StaticResource BookStatusTextStyle}"/>
                </StackLayout>
            </StackLayout>
        </ViewCell.View>
</ViewCell>
